# 三个目标选择器空白调试

- 会话：target-selectors-empty
- 状态：[OPEN]
- 症状：垃圾丢弃名单、食物白名单、搭路方块白名单点击后仅显示搜索框，没有候选项。
- 预期：弹窗显示可搜索、可选择的物品候选列表。

## 待验证假设

1. 三个选择器共用的点击或弹窗创建分支提前返回或抛出异常。
2. 候选集合或注册表来源与 26.1.2 API 不兼容。
3. 共用筛选谓词过滤掉全部候选项。
4. 候选控件已创建，但布局尺寸或渲染状态使其不可见。
5. 基类对三类设置的 GUI 转换逻辑与调用方式不匹配。

## 证据

- 待采集。

## 结论

- 待确认。
